# Release channels

`stable.json` and `preview.json` are machine-readable channel records. Both remain unavailable until a reviewed release is published. Channel promotion must point to an immutable version and never discover or execute an arbitrary latest tag.
