# Release artifacts

This directory is populated only by the approved release workflow. Generated artifacts are not committed to `main`; published assets are attached to immutable GitHub release tags together with their manifest, SHA-256 digest, SBOM, and release notes.
