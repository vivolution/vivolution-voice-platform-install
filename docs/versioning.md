# Versioning

Release candidates use `v0.1.0-rcN`. A failed candidate is replaced by a new RC number. Final `v0.1.0` is published only after the three-host proof succeeds. Published tags and assets are never rewritten.
